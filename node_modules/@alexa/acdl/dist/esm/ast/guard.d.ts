import { Node } from "./node";
/**
 * Generates a function that applies type assertions based on the `kind` of a node.
 *
 * @param kinds list of kind tags to guard for.
 */
export declare function guard<Kinds extends Node["kind"][]>(...kinds: Kinds): (node: any) => node is Extract<Node, {
    kind: Kinds[number];
}>;
//# sourceMappingURL=guard.d.ts.map