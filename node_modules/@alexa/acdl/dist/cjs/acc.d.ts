#!/usr/bin/env node
import "source-map-support/register";
import { AskCliConfig } from "./ask-cli-config";
/**
 * Runs the command
 * @param command command to run
 * @param argv command line arguments
 * @param localNpmVersion current npm package version
 * @param askCliConfig config file information
 * A promossed that resolves when the command is done
 */
export declare function runCommand(command: string, argv: any, localNpmVersion: string, askCliConfig: AskCliConfig): Promise<void>;
//# sourceMappingURL=acc.d.ts.map