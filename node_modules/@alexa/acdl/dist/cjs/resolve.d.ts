import { Project } from "./project";
import * as ask from "./ast";
import { TypeChecker } from "./checker";
/**
 * Walks over all modules in the Project and performs all type and argument inference on the tree once to avoid repeating this cost later.
 *
 * @param project
 * @returns a project where all modules
 */
export declare function resolveProject(project: Project): Project;
export declare function resolveModule(mod: ask.Module, checker: TypeChecker): ask.Module;
export declare function resolveExpr<N extends ask.Node>(expr: N, checker: TypeChecker): N;
//# sourceMappingURL=resolve.d.ts.map